<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The name of the resource to use for the settings page.  This always refers
    	 to an xml resource, so only the name, with no extension, is necessary. For  
    	 example, "settings" resolves to "res/xml/settings.xml"-->
	<string name="config_settingsResource">settings</string>
	
	<!-- The asset file (relative to the assets directory). -->
	<string name="config_questionsFile">quiz-example.xml</string>
	
</resources>
